# Security Policy

Windows Cleaner 会处理永久删除操作。请负责任地报告安全问题，尤其是以下类型：

- 保护目录或用户补充保护路径可被绕过；
- 未经明确批准即可执行删除；
- `PLAN_TOKEN` 可被复用、篡改或用于不同目标；
- 路径规范化、Provider 或 reparse point 检查失效；
- 预览后内容发生变化仍能继续删除；
- 能够触发任意路径删除或权限提升。

## 报告方式

优先使用本仓库 GitHub 页面中的 **Security → Report a vulnerability** 私下提交报告。请附上受影响版本或提交、Windows 与 PowerShell 版本、最小复现步骤、预期行为和实际行为。

在维护者完成初步评估前，请不要公开漏洞利用细节，也不要在真实个人数据或生产设备上验证删除问题。可以使用新建的临时测试目录进行最小化复现。

普通功能建议和不涉及安全影响的缺陷可以提交公开 Issue。
